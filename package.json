{
  "name": "academic-homepage-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "dev:demo": "turbo run dev --filter=@academic-homepage/demo",
    "sanity:dev": "turbo run sanity:dev --filter=@academic-homepage/demo",
    "build": "turbo run build",
    "build:demo": "turbo run build --filter=@academic-homepage/demo",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
